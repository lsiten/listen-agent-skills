---
name: {{skill-name}}
description: {{完整描述skill功能、使用场景和核心特性，1-1024字符}}
version: 1.0.0
author: ""
tags: ["{{tag1}}", "{{tag2}}", "{{tag3}}", "{{tag4}}"]
---

# {{Skill名称}} Skill

你是一个专门处理{{任务类型}}的AI助手，具备{{核心能力详细描述}}。

## 任务概述

本技能用于{{详细功能描述}}，通过{{实现方式/技术手段}}完成{{目标任务}}。工作流程分为{{阶段数量}}个阶段：{{阶段列表，如"首次使用检查（阶段0）、准备与指令生成（阶段1）、综合分析（阶段2）"}}。

**重要说明**：
- {{重要说明1，如"主要的工作应该由AI通过独立思考来完成"}}
- {{重要说明2，如"关键原则：数据必须先在本地进行分析和处理"}}

## 核心能力

- 🔍 **{{能力1名称}}** - {{能力1详细说明}}
- 📊 **{{能力2名称}}** - {{能力2详细说明}}
- 🧠 **{{能力3名称}}** - {{能力3详细说明}}
- ⏰ **{{能力4名称}}** - {{能力4详细说明}}
- 🔄 **{{能力5名称}}** - {{能力5详细说明}}
- 🔎 **{{能力6名称}}** - {{能力6详细说明}}
- 💻 **{{能力7名称}}** - {{能力7详细说明}}
- 📝 **{{能力8名称}}** - {{能力8详细说明}}

## 整体工作流程

⚠️ **关键原则：必须按顺序执行，不能跳过任何阶段！**

```
用户输入{{输入类型描述}}
    ↓
【阶段0：{{阶段0名称}}】⚠️ 必须首先执行！
    ├─ 🔍 {{检查任务1}}
    ├─ 🔍 {{检查任务2}}
    ├─ ❌ 如果{{条件}}：
    │   ├─ {{处理步骤1}}
    │   └─ {{处理步骤2}}
    └─ ✅ 如果{{条件}}：直接进入阶段1
    ↓
【阶段1：{{阶段1名称}}】⚠️ 必须在阶段0完成后执行！
    ├─ ⚠️ 前置检查：{{检查内容}}
    ├─ {{任务1}}
    ├─ {{任务2}}
    └─ {{任务3}}
    ↓
【{{中间步骤名称}}】
    ├─ {{步骤1}}
    ├─ {{步骤2}}
    └─ {{步骤3}}
    ↓
【阶段2：{{阶段2名称}}】
    ├─ {{任务1}}
    ├─ {{任务2}}
    └─ {{输出结果}}
```

### ⚠️ 执行顺序强制约束

**绝对禁止**：
- ❌ {{禁止事项1}}
- ❌ {{禁止事项2}}
- ❌ {{禁止事项3}}

**必须遵守**：
- ✅ {{遵守事项1}}
- ✅ {{遵守事项2}}
- ✅ {{遵守事项3}}

## 阶段0：{{阶段0名称}}

⚠️ **这是整个工作流程的第一步，必须在任何{{主要任务}}之前完成！**

### 前置检查（每次执行前必须执行）

在开始任何任务之前，**必须首先执行**以下检查：

```
1. 检查 {{配置文件路径1}} 是否存在
2. 检查 {{配置文件路径2}} 是否存在

如果两个文件都存在 → 跳过阶段0，直接进入阶段1
如果任一文件不存在 → 必须执行完整的阶段0流程
```

### 主要任务

- **{{任务1名称}}**：{{任务1说明}}
- **{{任务2名称}}**：{{任务2说明}}
- **{{任务3名称}}**：{{任务3说明}}

### 关键配置信息

⚠️ **重要**：以下信息文件每次生成的格式可能不同，但必须字段必须包含，尽量要的字段尽量包含，其他补充字段可随意。

#### `{{配置文件1路径}}` 字段规范

**必须字段**（必须包含）：
- `{{字段1}}`：{{字段1说明}}（{{数据类型}}）
- `{{字段2}}`：{{字段2说明}}（{{数据类型}}）
- `{{字段3}}`：{{字段3说明}}（{{数据类型}}）

**尽量要的字段**（尽量包含）：
- `{{字段4}}`：{{字段4说明}}（{{数据类型}}）
- `{{字段5}}`：{{字段5说明}}（{{数据类型}}）

**其他补充字段**（可随意）：
- 可以根据实际情况添加其他字段，如：`{{示例字段}}`等

#### `{{配置文件2路径}}` 字段规范

**必须字段**（必须包含）：
- `{{字段1}}`：{{字段1说明}}
- `{{字段2}}`：{{字段2说明}}

**尽量要的字段**（尽量包含）：
- `{{字段3}}`：{{字段3说明}}

### 执行步骤

1. **{{步骤1名称}}**
   - {{步骤1详细说明}}
   - {{具体操作}}

2. **{{步骤2名称}}**
   - {{步骤2详细说明}}
   - {{具体操作}}

## 阶段1：{{阶段1名称}}

### 主要任务

1. {{任务1}}
2. {{任务2}}
3. {{任务3}}
4. {{任务4}}
5. {{任务5}}

⚠️ **重要**：必须生成`{{输出文件路径}}`文件，包含{{文件内容描述}}。

### {{关键处理逻辑名称}}

从{{输入源}}中提取以下信息：

- **{{字段1}}**：{{提取规则说明}}
- **{{字段2}}**：{{提取规则说明}}
- **{{字段3}}**：{{提取规则说明}}

⚠️ **{{特殊情况}}处理**：

**常见场景：{{场景名称}}**
- {{场景说明}}
- {{处理方式}}

**处理流程**：

1. **{{步骤1}}**：
   - {{详细说明}}

2. **{{步骤2}}**：
   - {{详细说明}}

3. **⚠️ {{特殊情况}}时必须停止流程**：
   - {{条件说明}}
   - {{处理方式}}

### 常见场景处理指南

根据{{输入类型}}的不同类型，采用不同的处理策略：

#### 场景1：{{场景1名称}}

**特征**：{{场景特征描述}}

**处理策略**：
1. {{策略步骤1}}
2. {{策略步骤2}}
3. {{策略步骤3}}

#### 场景2：{{场景2名称}}

**特征**：{{场景特征描述}}

**处理策略**：
1. {{策略步骤1}}
2. {{策略步骤2}}

### {{核心处理规则名称}}

⚠️ **重要约束**：{{约束说明}}。

**{{规则类型}}优先级（从高到低）**：

1. **{{优先级1名称}}**（最高优先级）
   - {{优先级1说明}}
   - 计算方式：`{{计算公式或规则}}`

2. **{{优先级2名称}}**（次优先级）
   - {{优先级2说明}}

3. **{{优先级3名称}}**（第三优先级）
   - {{优先级3说明}}

**处理策略**：

1. **{{策略1名称}}**：
   - {{策略说明}}
   - ⚠️ **重要**：{{重要提示}}

2. **切换条件**：
   - ⚠️ **关键逻辑**：{{切换逻辑说明}}
   - **提前终止**：{{终止条件}}

### {{指令生成}}

#### {{工具/接口}}选择优先级

**推荐优先级（从高到低）：**

1. **{{工具1}}** - {{推荐原因}}
2. **{{工具2}}** - {{使用场景}}
3. **{{工具3}}** - {{使用场景}}

#### 指令结构

生成的 `{{指令文件路径}}` 应包含：

```json
{
  "{{字段1}}": "{{示例值}}",
  "{{字段2}}": {
    "{{子字段1}}": {{示例值}},
    "{{子字段2}}": {{示例值}}
  },
  "{{字段3}}": [
    {
      "{{数组项字段1}}": {{示例值}},
      "{{数组项字段2}}": "{{示例值}}",
      "{{数组项字段3}}": "{{说明}}"
    }
  ]
}
```

## 阶段2：{{阶段2名称}}

### 主要任务

1. **{{任务1}}**
   - ⚠️ **必须**：{{必须说明}}
2. **{{任务2}}**
3. **本地分析**（重要！）：
   - ⚠️ **关键原则**：{{关键原则说明}}
   - 在本地对结果进行统计分析：
     - {{分析项1}}
     - {{分析项2}}
     - {{分析项3}}
   - 生成关键信息摘要
4. **{{任务3}}**
5. **{{任务4}}**
6. **生成{{输出名称}}**（仅在{{条件}}时）
7. **输出文档**（必须生成）：
   - 如果{{条件1}}：生成 `{{文档1路径}}`
   - 如果{{条件2}}：生成 `{{文档2路径}}`

### {{核心分析逻辑}}

**分析流程**：
1. {{步骤1}}
2. {{步骤2}}
3. {{步骤3}}
4. 分析结果，判断{{判断维度}}级别：
   - 🔴 严重（critical）：{{严重条件}}
   - 🟠 高（high）：{{高级条件}}
   - 🟡 中等（medium）：{{中等条件}}
   - 🟢 轻微（low）：{{轻微条件}}
   - ✅ {{正常状态}}：{{正常条件}}

## {{数据结构章节名称}}

### {{数据类型1}}

| 字段名 | 数据类型 | 说明 | 使用场景 |
|--------|---------|------|---------|
| `{{字段1}}` | {{类型}} | {{说明}} | {{场景}} |
| `{{字段2}}` | {{类型}} | {{说明}} | {{场景}} |
| `{{字段3}}` | {{类型}} | {{说明}} | {{场景}} |

### {{数据类型2}}

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| `{{字段1}}` | {{类型}} | {{说明}} |
| `{{字段2}}` | {{类型}} | {{说明}} |

## AI执行方式

### 第一步：理解任务

当用户提出任务时，你应该：

1. **理解需求** - {{理解方式说明}}
2. **识别输入格式** - {{识别方式说明}}
3. **提取关键信息** - {{提取方式说明}}
4. **确定执行路径** - {{确定方式说明}}

### 第二步：规划执行（⚠️ 必须严格按顺序执行）

**🔴 第一优先级：{{检查项名称}}（必须首先执行！）**

1. **检查配置文件是否存在**：
   ```
   检查1: {{配置文件1}} 是否存在？
   检查2: {{配置文件2}} 是否存在？
   ```

   - **如果任一文件不存在**：
     - ⚠️ **立即停止**，不要{{禁止操作}}
     - 必须先执行完整的阶段0流程
     - 只有在配置文件都生成完成后，才能继续执行下一步

   - **如果两个文件都存在**：继续执行下一步

**🟡 第二优先级：{{任务项名称}}（配置文件存在后才能执行）**

2. **{{步骤1}}** - {{步骤1说明}}
3. **{{步骤2}}** - {{步骤2说明}}
4. **{{步骤3}}** - {{步骤3说明}}

**❌ 禁止行为**：
- {{禁止行为1}}
- {{禁止行为2}}
- {{禁止行为3}}

### 第三步：执行

1. **按优先级执行**：
   - {{执行说明}}

2. **执行{{工具/操作}}** - {{执行说明}}
   - ⚠️ **重要**：{{重要提示}}
   - 推荐{{推荐方式}}

3. **验证结果**：
   - ⚠️ **首先检查是否成功**：{{检查方式}}
   - {{验证步骤}}

4. **保存结果** - 将结果保存到`{{结果文件路径}}`

### 第四步：综合分析

1. **本地分析结果**（重要！）：
   - ⚠️ **关键原则**：{{关键原则}}
   - {{分析步骤}}

2. **AI分析关键信息** - {{分析说明}}
3. **{{分析任务1}}** - {{任务说明}}
4. **{{分析任务2}}** - {{任务说明}}
5. **生成{{输出}}** - {{输出说明}}

### 第五步：结果处理

1. **验证文件完整性** - 确保所有必需的文件都已生成：
   - ✅ `{{文件1}}` - {{文件1说明}}
   - ✅ `{{文件2}}` - {{文件2说明}}
   - ✅ `{{文件3}}` - {{文件3说明}}
2. **总结结果** - {{总结说明}}
3. **提供反馈** - {{反馈说明}}

## 文档模板

### {{输出文档1}}模板（{{使用条件1}}时使用）

```markdown
# {{文档标题}}

## 基本信息

| 项目 | 内容 |
|------|------|
| {{字段1}} | {{值}} |
| {{字段2}} | {{值}} |

## {{章节1}}

{{章节内容说明}}

## {{章节2}}

{{章节内容说明}}

## {{章节3}}

{{章节内容说明}}
```

### {{输出文档2}}模板（{{使用条件2}}时使用）

```markdown
# {{文档标题}}

## 基本信息

| 项目 | 内容 |
|------|------|
| {{字段1}} | {{值}} |
| {{字段2}} | ⚠️ {{状态}} |

## {{章节1}}

{{章节内容说明}}

## {{章节2}}

{{章节内容说明}}
```

## 配置选项

| 参数 | 类型 | 默认值 | 描述 |
|------|------|--------|------|
| `--{{参数1}}` | string | - | {{描述}} |
| `--{{参数2}}` | string | - | {{描述}} |
| `--{{参数3}}` | string | - | {{描述}} |

## 使用示例

### 示例1：{{场景1名称}}

**用户输入**：{{输入描述}}

**AI执行流程**：
1. **{{步骤1}}** - {{说明}}
2. **{{步骤2}}** - {{说明}}
3. **{{步骤3}}** - {{说明}}
4. **{{步骤4}}** - {{说明}}

### 示例2：{{场景2名称}}

**用户输入**：{{输入描述}}

**AI执行流程**：
1. **{{步骤1}}** - {{说明}}
2. **{{步骤2}}** - {{说明}}
3. **{{步骤3}}** - {{说明}}

## 最佳实践

1. **{{实践1名称}}**
   - {{建议1}}
   - {{建议2}}

2. **{{实践2名称}}**
   - {{建议1}}
   - {{建议2}}

3. **{{实践3名称}}**
   - {{建议1}}
   - {{建议2}}

4. **{{实践4名称}}**
   - {{建议1}}
   - {{建议2}}

## 故障排除

### 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| {{问题1}} | {{原因1}} | {{解决方案1}} |
| {{问题2}} | {{原因2}} | {{解决方案2}} |
| {{问题3}} | {{原因3}} | {{解决方案3}} |
| {{问题4}} | {{原因4}} | {{解决方案4}} |

### 调试模式

在执行过程中，如果遇到问题，可以：

1. **{{检查项1}}** - {{检查说明}}
2. **{{检查项2}}** - {{检查说明}}
3. **{{检查项3}}** - {{检查说明}}
4. **{{检查项4}}** - {{检查说明}}

## 辅助工具说明

- **{{工具1}}** - {{工具1说明}}
- **{{工具2}}** - {{工具2说明}}
- **{{工具3}}** - {{工具3说明}}

**重要原则**：
- {{原则1}}
- {{原则2}}

## 扩展功能

- **{{扩展1}}** - {{扩展1说明}}
- **{{扩展2}}** - {{扩展2说明}}
- **{{扩展3}}** - {{扩展3说明}}

## 技术架构

- **{{组件1}}**: {{组件1说明}}
- **{{组件2}}**: {{组件2说明}}
- **{{组件3}}**: {{组件3说明}}
- **{{组件4}}**: {{组件4说明}}
