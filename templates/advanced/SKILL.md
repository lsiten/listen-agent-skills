---
name: {{skill-name}}
description: {{详细描述skill功能和使用场景，1-1024字符}}
version: 1.0.0
author: ""
tags: ["{{tag1}}", "{{tag2}}", "{{tag3}}"]
---

# {{Skill名称}} Skill

你是一个专门处理{{任务类型}}的AI助手，具备{{核心能力概述}}。

## 任务概述

本技能用于{{详细功能描述}}，支持{{支持的功能/格式列表}}。

## 核心能力

- 📊 **{{能力1名称}}** - {{能力1详细说明}}
- 📈 **{{能力2名称}}** - {{能力2详细说明}}
- 📝 **{{能力3名称}}** - {{能力3详细说明}}
- 🔍 **{{能力4名称}}** - {{能力4详细说明}}
- 🎨 **{{能力5名称}}** - {{能力5详细说明}}

## 系统要求

### 硬件要求
- **处理器**: {{CPU要求，如"现代CPU（推荐多核）"}}
- **内存**: {{内存要求，如"≥4GB RAM（推荐8GB+）"}}
- **存储**: {{存储要求，如"≥500MB可用空间"}}

### 软件依赖
- {{依赖1，如"Python 3.8+"}}
- {{依赖2，如"pandas (数据处理)"}}
- {{依赖3，如"numpy (数值计算)"}}

## 执行步骤

### 第一步：环境准备

安装所有必需的依赖：

```bash
cd skills/{{skill-name}}
./scripts/install_dependencies.sh
```

或者手动安装：

```bash
{{手动安装命令，如"pip install pandas numpy matplotlib"}}
```

### 第二步：准备数据/配置

{{数据或配置准备说明}}

**支持的格式**：
- {{格式1}}：`.{{扩展名}}`
- {{格式2}}：`.{{扩展名}}`

**数据要求**：
- {{要求1，如"第一行为列名"}}
- {{要求2，如"数值型数据应为数字格式"}}

**示例数据格式**：
```{{格式}}
{{数据示例}}
```

### 第三步：执行任务

#### 方法1：{{基础方法名称}}

```bash
python scripts/{{main_script}}.py \
  --{{参数1}} "{{参数1示例值}}" \
  --{{参数2}} "{{参数2示例值}}"
```

#### 方法2：{{高级方法名称}}（如有）

```bash
python scripts/{{main_script}}.py \
  --{{参数1}} "{{参数1示例值}}" \
  --{{高级参数}} "{{高级参数值}}"
```

#### 方法3：{{特殊方法名称}}（如有）

```bash
python scripts/{{main_script}}.py \
  --{{参数1}} "{{参数1示例值}}" \
  --{{特殊参数}} "{{特殊参数值}}"
```

### 第四步：查看结果

{{结果查看说明，如"脚本会自动在浏览器中打开生成的HTML报告"}}

## 参数说明

| 参数 | 类型 | 必需 | 默认值 | 描述 |
|------|------|------|--------|------|
| `--{{参数1}}` | string | 是 | - | {{参数1描述}} |
| `--{{参数2}}` | string | 否 | `{{默认值}}` | {{参数2描述}} |
| `--{{参数3}}` | string | 否 | - | {{参数3描述}} |
| `--{{参数4}}` | string | 否 | `auto` | {{参数4描述}} |
| `--{{参数5}}` | flag | 否 | `true` | {{参数5描述}} |

## {{特定功能说明}}（如有）

<!-- 如果skill有特定的功能逻辑需要说明，如"分析模型选择逻辑"、"参数调优映射"等 -->

### {{功能逻辑名称}}

{{功能逻辑说明}}

| {{条件列}} | {{结果列}} | {{说明列}} |
|-----------|-----------|-----------|
| {{条件1}} | {{结果1}} | {{说明1}} |
| {{条件2}} | {{结果2}} | {{说明2}} |

## 使用示例

### 示例1：{{基础使用场景}}

```bash
# {{示例说明}}
python scripts/{{main_script}}.py \
  --{{参数1}} "{{示例值1}}" \
  --{{参数2}} "{{示例值2}}" \
  --{{参数3}} "{{示例值3}}"
```

### 示例2：{{高级使用场景}}

```bash
# {{示例说明}}
python scripts/{{main_script}}.py \
  --{{参数1}} "{{示例值1}}" \
  --{{高级参数}} "{{高级示例值}}"
```

### 示例3：{{特殊使用场景}}

```bash
# {{示例说明}}
python scripts/{{main_script}}.py \
  --{{参数1}} "{{示例值1}}" \
  --{{特殊参数}} "{{特殊值}}"
```

## 最佳实践

1. **{{实践类别1，如"数据准备"}}**
   - {{具体建议1}}
   - {{具体建议2}}

2. **{{实践类别2，如"参数选择"}}**
   - {{具体建议1}}
   - {{具体建议2}}

3. **{{实践类别3，如"结果解读"}}**
   - {{具体建议1}}
   - {{具体建议2}}

4. **{{实践类别4，如"报告使用"}}**
   - {{具体建议1}}
   - {{具体建议2}}

## 故障排除

### 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| {{问题1，如"文件读取失败"}} | {{原因1，如"文件路径错误或格式不支持"}} | {{解决方案1}} |
| {{问题2，如"依赖包缺失"}} | {{原因2，如"未安装必需的Python包"}} | {{解决方案2，如"运行install_dependencies.sh"}} |
| {{问题3，如"内存不足"}} | {{原因3，如"数据量过大"}} | {{解决方案3，如"分批处理或增加系统内存"}} |
| {{问题4}} | {{原因4}} | {{解决方案4}} |

### 调试模式

启用详细日志输出：

```bash
python scripts/{{main_script}}.py \
  --{{参数1}} "{{值}}" \
  --verbose
```

## 扩展功能

- **{{扩展1，如"批量处理"}}** - {{扩展1说明}}
- **{{扩展2，如"自定义模板"}}** - {{扩展2说明}}
- **{{扩展3，如"导出功能"}}** - {{扩展3说明}}
- **{{扩展4，如"API集成"}}** - {{扩展4说明}}

## 技术架构

- **{{组件1，如"数据处理"}}**: {{技术说明，如"pandas + numpy"}}
- **{{组件2，如"可视化"}}**: {{技术说明，如"matplotlib + seaborn"}}
- **{{组件3，如"报告生成"}}**: {{技术说明，如"jinja2模板引擎"}}

## 注意事项

- {{注意事项1，如"确保数据文件路径正确且可访问"}}
- {{注意事项2，如"大数据文件（>100MB）可能需要较长处理时间"}}
- {{注意事项3，如"建议定期备份分析结果和原始数据"}}
