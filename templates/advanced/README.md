# {{name}}

## 描述

{{description}}

## 适用场景

本模板适用于**中等复杂度**的skill，例如：
- 数据分析与报告生成
- 需要多步骤执行的任务
- 需要安装依赖和环境准备的任务
- 带有参数配置的脚本执行

**参考实例**: `survey-data-analysis`

## 模板特点

- 📊 **结构化流程** - 清晰的多步骤执行流程
- 🔧 **脚本辅助** - 使用Python等脚本处理数据
- 📋 **参数化配置** - 支持丰富的命令行参数
- 📝 **完整文档** - 包含故障排除和最佳实践

## 目录结构

```
{{skill-name}}/
├── SKILL.md                    # 技能定义文件（必须）
├── README.md                   # 说明文档
├── scripts/                    # 执行脚本
│   ├── install_dependencies.sh # 依赖安装脚本
│   └── {{main_script}}.py      # 主执行脚本
└── resources/                  # 资源文件（可选）
    └── templates/              # 模板文件
```

## 快速开始

1. 复制本模板目录到 `skills/{{your-skill-name}}/`
2. 编辑 `SKILL.md`，替换所有 `{{placeholder}}`
3. 实现 `scripts/` 目录下的脚本
4. 更新此 `README.md`
5. 测试完整流程

## SKILL.md 结构说明

```markdown
---
name: skill-name
description: 详细功能描述
version: 1.0.0
author: ""
tags: ["tag1", "tag2", "tag3"]
---

# 技能名称 Skill
## 任务概述              # 详细功能描述
## 核心能力              # 4-5个核心能力
## 系统要求              # 硬件要求和软件依赖
## 执行步骤              # 四步流程：环境准备→数据准备→执行任务→查看结果
## 参数说明              # 命令行参数表格
## 特定功能说明          # 特定逻辑规则（如有）
## 使用示例              # 基础/高级/特殊场景示例
## 最佳实践              # 分类建议
## 故障排除              # 常见问题表格和调试模式
## 扩展功能              # 可扩展能力
## 技术架构              # 技术栈说明
## 注意事项              # 重要提醒
```

## 何时选择此模板

✅ **适合**：
- 需要多步骤执行的数据处理任务
- 需要安装软件依赖的任务
- 需要丰富参数配置的任务
- 生成报告或分析结果的任务

❌ **不适合**：
- 简单的API调用（使用 `basic` 模板）
- 需要复杂多阶段工作流的任务（使用 `complex` 模板）
- 需要状态管理和流程控制的任务

## 脚本开发指南

### install_dependencies.sh

```bash
#!/bin/bash
# 检查并安装依赖
pip install pandas numpy matplotlib 2>/dev/null || {
    echo "请手动安装依赖: pip install pandas numpy matplotlib"
    exit 1
}
echo "依赖安装完成"
```

### 主脚本结构

```python
#!/usr/bin/env python3
import argparse

def main():
    parser = argparse.ArgumentParser(description='{{skill描述}}')
    parser.add_argument('--input', required=True, help='输入文件路径')
    parser.add_argument('--output', default='output', help='输出目录')
    parser.add_argument('--verbose', action='store_true', help='详细输出')

    args = parser.parse_args()
    # 实现逻辑...

if __name__ == '__main__':
    main()
```

## 注意事项

- 确保脚本有可执行权限：`chmod +x scripts/*.sh`
- 参数说明表格要完整，包含类型、必需性、默认值
- 故障排除表格覆盖常见问题
- 使用示例应覆盖基础、高级、特殊场景
