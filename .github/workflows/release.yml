name: Release and Publish

on:
  push:
    tags:
      - 'v*'  # 触发条件：推送以 v 开头的标签，如 v1.0.0

permissions:
  contents: write  # 允许创建 release
  packages: write  # 允许发布包

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build project
        run: npm run build
        
      - name: Run tests (if available)
        run: npm test --if-present
        
      - name: Extract version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
        
      - name: Verify version matches tag
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          TAG_VERSION="${{ steps.get_version.outputs.VERSION }}"
          echo "Package version: $PACKAGE_VERSION"
          echo "Tag version: $TAG_VERSION"
          if [ "$PACKAGE_VERSION" != "$TAG_VERSION" ]; then
            echo "❌ Version mismatch: package.json has $PACKAGE_VERSION but tag is v$TAG_VERSION"
            exit 1
          fi
          echo "✅ Version matches: $PACKAGE_VERSION"
          
      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ steps.get_version.outputs.VERSION }}
          body: |
            ## 🚀 Release ${{ steps.get_version.outputs.VERSION }}
            
            ### 📦 Installation
            ```bash
            npm install -g listen-agent@${{ steps.get_version.outputs.VERSION }}
            ```
            
            ### 🎯 Features
            - ✅ CLI工具支持 setup/init/list/create 命令
            - ✅ 支持10+种AI助手 (Claude, Cursor, Windsurf, Kiro等)
            - ✅ 智能检测AI助手类型并自动安装
            - ✅ 完全符合Agent Skills开放标准
            - ✅ 支持模板化skill创建
            - ✅ 完整的TypeScript类型支持
            
            ### 📋 Included Skills
            - **企业微信通知** - 支持发送文本、markdown、图片等多种消息类型
            - **Mac M4 LoRA训练** - 专为Mac M4芯片优化的LoRA自动化训练工具
            
            ### 🔗 Links
            - [NPM Package](https://www.npmjs.com/package/listen-agent)
            - [Documentation](https://github.com/lsiten/listen-agent-skills#readme)
            
            ### 📋 Changes
            See the commit history for detailed changes in this release.
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}